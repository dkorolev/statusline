# `statusline`

I want to see:

* Model.
* Effort.
* Context use %.
* Dir.
* Branch, if on git.
* HEAD commit SHA (first seven hex digits), plus an afterisk if dirty.
* Usage limits and their reset times.
